name: server-cd

on:
  push:
    branches: [develop, main]
    paths:
      - 'server/**'
      - '.github/workflows/server-cd.yml'

  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: server-cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  server-cd:
    runs-on: ubuntu-latest
    if: ${{ (github.ref == 'refs/heads/develop' && vars.ENABLE_API_STAGING_DEPLOY == 'true') ||
      (github.ref == 'refs/heads/main' && vars.ENABLE_API_PROD_DEPLOY == 'true')}}
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set lowercase owner
        id: repo
        run: echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Set environment values
        id: vars
        run: |
          if [[ "${GITHUB_REF_NAME}" == "develop" ]]; then
            echo "image_tag=staging" >> "$GITHUB_OUTPUT"
          elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "image_tag=prod" >> "$GITHUB_OUTPUT"
          else
            echo "Unsupported branch: ${GITHUB_REF_NAME}"
            exit 1
          fi

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.repo.outputs.owner_lc }}/server:${{ steps.vars.outputs.image_tag }}
            ghcr.io/${{ steps.repo.outputs.owner_lc }}/server:sha-${{ github.sha }}
          labels: |
            org.opencontainers.image.title=api
            org.opencontainers.image.description=API image
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Trigger Render deploy
        run: |
          test -n "${{ secrets.RENDER_DEPLOY_HOOK_API }}"
          curl -fsS -X POST "${{ secrets.RENDER_DEPLOY_HOOK_API }}"
